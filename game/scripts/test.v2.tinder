set item to "None"
const ITEMS = [ "a", "b", "c", "d", "e" ]
foreach item in ITEMS
  set A to True

  if item is "a"
    call debug("Nope!")
  else if item is "b"
    call debug("Yep!")
  else
    call debug("Yoooo!")
  endif
endfor

set a, b to 0, 1
call debug(a)
call debug(b)

for i = 0; i < len(ITEMS); inc i
  call debug(i from ITEMS)
endfor
