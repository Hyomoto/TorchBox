`` -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
  from text import pad, column
#start
  call enter("tools/header", {a: USER.nickname, b: "'s Status"})
  const attr = USER.attributes
  const modd = USER.modifiers
  write column(["Profession", modd.profession or "Unemployed"], 15, { separator: "|", pad: "  " })
  ""
  input "  <`11MORE`3>"
# end
  call exit()
